/**
 * Represents a basic interface for the Socket feature.
 * Since this feature was created without a Prisma model, you can extend
 * this interface with your own properties as needed.
 */
export interface SocketResponse {
  message: string;
}
